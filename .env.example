# =============================================================================
#  Root .env.example — Supabase Edge Function Secrets
#  These are set via the Supabase CLI, NOT via a .env file.
#  Kept here as a reference.
#
#  Set secrets:
#    supabase secrets set KEY=value --project-ref YOUR_REF
#  List secrets:
#    supabase secrets list --project-ref YOUR_REF
# =============================================================================

# ── YouTube Data API (required for search & metadata) ─────────────────────────
# Obtain from: https://console.cloud.google.com/ → APIs & Services → Credentials
# Enable "YouTube Data API v3" for your project first.
YOUTUBE_API_KEY=AIzaSy...
# Optional: additional keys for rotation (prevents quota exhaustion)
YOUTUBE_API_KEY_2=AIzaSy...
YOUTUBE_API_KEY_3=AIzaSy...

# ── Supabase Service Role Key (server-side only — NEVER expose in browser) ────
# Found in: Supabase Dashboard → Settings → API → service_role key
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
