# Kiosk Interface — Environment Variables
# Copy this file to .env and fill in your values.

# ─── Supabase Connection ─────────────────────────────────────────────────────

# Your Supabase project URL (or http://localhost:54321 for local dev)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase anonymous (public) key — safe to expose in the browser.
# The kiosk uses anonymous access — no login required.
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ─── Player Instance ─────────────────────────────────────────────────────────

# UUID of the player instance this kiosk is requesting songs for.
# Must match the VITE_PLAYER_ID set in admin and player for the same jukebox.
VITE_PLAYER_ID=00000000-0000-0000-0000-000000000001
